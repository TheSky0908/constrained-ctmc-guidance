.idea/
.DS_Store
.ipynb_checkpoints/
__pycache__/

# dataset / huggingface caches
.hf_cache/
.data_cache/

# hydra per-run scratch
.hydra/
hydra_logs/

# model checkpoints / weights (the large files inside outputs/)
*.ckpt
*.pt
*.pth
*.safetensors
*.bin

# experiment runtime roots (checkpoints, raw sample/traj dumps, hydra run dirs).
# Curated summary CSVs live under experiments/<task>/results/ and ARE tracked.
outputs/
logs/
experiments/*/logs/

# raw lambda-trajectory dumps (large, reproducible). Kept on disk, not in git.
*_traj.json
**/*_traj.json

# wandb run dirs (large binary metadata, recreatable)
wandb/

# agent / aris scratch
.agents/
.aris/
watch_folder/

# python
*.pyc
*.pyo
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# editor / system
.vscode/
*.swp
*.swo
